export interface ICatalogEntityImmu {
  catalogId: string;
  compId: string;
}

export interface ICatalogEntity {
  companyCode: string;
  domainAlias: string;
}

export interface ICatalogData {
  pages: string[]
}